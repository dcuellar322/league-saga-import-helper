# Used for local development only. Production values should come from LeagueSaga's import session deep link.
LEAGUESAGA_API_BASE=http://localhost:15173
LEAGUESAGA_IMPORT_TOKEN=dev-only-token
LEAGUESAGA_ENABLE_COOKIE_DIAGNOSTICS=false
