{
  "name": "league-saga-import-helper-repo",
  "private": true,
  "version": "0.3.2",
  "description": "Secure desktop fantasy league import helper for LeagueSaga.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "npm run build:contract && npm -w apps/desktop run dev",
    "build": "npm run build:contract && npm -w apps/desktop run build",
    "make": "npm run build:contract && npm -w apps/desktop run make",
    "package": "npm run build:contract && npm -w apps/desktop run package",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "release:check-version": "node scripts/check-release-version.mjs",
    "smoke:production-upload": "node scripts/smoke-production-upload.mjs",
    "typecheck": "npm run build:contract && npm --workspaces run typecheck",
    "build:contract": "npm -w packages/import-contract run build && npm -w packages/import-contract run schema",
    "clean": "node scripts/clean.mjs",
    "quality": "npm run lint && npm run format:check && npm run test:coverage && npm run typecheck && npm run build",
    "make:msix": "pwsh -NoProfile -File scripts/build-msix.ps1",
    "test:msix": "node --test scripts/prepare-msix.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.3.3",
    "@vitest/coverage-v8": "^5.0.0",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.69.0",
    "vitest": "^5.0.0"
  }
}
